<hr style="clear:both;visibility:hidden;">
<h1 style="text-align: center;">Manage assessments</h1>
<div id="table"></div>

<% content_for :modal do %>
  <% render 'form', assessment:Assessment.new %>
<% end %>

<script>
    var table = new Tabulator("#table", {
        maxHeight: "85%",
        layout: "fitColumns",
        placeholder: "No Data Set",
        columns: [
            {title: "Subject ID", field: "subject_id", sorter: "number", width: 200, editor: true},
            {title: "Subject Title", field: "subject_id",  formatter: subjectFormatter, sorter: "string", width: 200, editor: false},
            {title: "Assessment Name", field: "name", sorter: "number", editor: true},
            {formatter:"buttonCross", width:40, align:"center", cellClick:deleteItem}
        ],
        cellEdited: function (cell) {
            // This callback is called any time a cell is edited.
            console.log(cell.getRow().getData())
            $.ajax({

                url: '/assessments/'+cell.getRow().getData().id,
                method: 'PATCH',
                dataType: 'json',
                data: {assessment: cell.getRow().getData()},
                success: function () {
                    toastr.success('Edit was successful!')
                    table.replaceData()
                },
                error: function () {
                    toastr.error('Incorrect input or insufficient access!')
                    table.replaceData()
                }

            })
        },
    });

    $(document).ready(function () {
        table.setData("/assessments");
    });

</script>
<button class="btn btn-primary" type="submit" onclick="MicroModal.show('modal-1')">New Assessment</button>
<button class="btn btn-primary" onclick="table.replaceData()">Refresh Data</button>